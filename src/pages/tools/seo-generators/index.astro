---
import HubPage from '@/components/HubPage.astro';
import { getToolHub } from '@/data/hubs';

const hub = getToolHub('seo-generators');
if (!hub) return Astro.redirect('/404/');
---
<HubPage hub={hub} />
