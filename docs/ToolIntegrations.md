# Filesystem Tools & Integrations

This guide details tool calling bindings for file operations, folder listings, and path checks.

---

## 1. File Actions

* **`readFileTool()`**: Loads local files securely.
* **`writeFileTool()`**: Saves text data to files.
* **Path Traversal Shield**: Checks inputs to block access attempts outside target boundaries.
