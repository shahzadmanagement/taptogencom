# Enterprise Administration Console

This document describes settings management, stats aggregators, and activity logs trackers.

---

## 1. System Overview

We fetch node environment metrics dynamically using `systemOverview.ts`:
* Node compilation build parameters.
* Memory heap limit scopes.
